============
Installation
============

At the command line::

    $ easy_install django-mr-benn

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mr-benn
    $ pip install django-mr-benn
